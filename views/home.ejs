
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin page</title>
    <script>
        function filieresRedirect(user){
            location.href= '/filieres'
        }
    </script>
    <style>
        button{
            margin-top: 3rem;
            padding: 1rem;
        }
    </style>
</head>
<body>
    <% if(user){ %>
    <h1>Welcome <%= user.username %> <%= user.role  %> to Homepage</h1>
    <button onclick="filieresRedirect('<%= JSON.stringify(user) %>')">Click here</button>
    <form action="/connexion" method="post">
        <button type="submit">Déconnexion</button>
    </form>
    <% }else{ %>
    <a href="/connexion">Connexion</a>
    <% } %>
</body>
</html>